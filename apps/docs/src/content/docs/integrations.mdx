---
title: SignetPad integrations
description: Import paths for the SignetPad React, Vue, Svelte, and React Native adapters and ready-made SignaturePad components.
section: integrations
order: 30
---

import { LivePreview } from '../../components/LivePreview.tsx';

Install `signetpad` once. Import the smallest entry for your app. For the common case, that entry now includes a `SignaturePad` you can drop in.

<LivePreview client:load example="draw" />

## Adapter matrix

| App                | Import path                  | Start here                          | Guide                              |
| ------------------ | ---------------------------- | ----------------------------------- | ---------------------------------- |
| React, Vite, Remix | `signetpad/react`            | `<SignaturePad />`                  | [React](/docs/react)               |
| Next.js            | `signetpad/react`            | `<SignaturePad />` in a client file | [Next.js](/docs/nextjs)            |
| React Native       | `signetpad/react-native-svg` | `<SignaturePad />`                  | [React Native](/docs/react-native) |
| Vue 3, Nuxt        | `signetpad/vue`              | `<SignaturePad />`                  | [Vue](/docs/vue)                   |
| Svelte, SvelteKit  | `signetpad/svelte`           | `createSignaturePadAction()`        | [SvelteKit](/docs/sveltekit)       |

There is no extra package for Next.js, Nuxt, or SvelteKit. Next.js uses React’s client boundary, Nuxt uses Vue’s client-only boundary, and SvelteKit mounts the action only in the browser.

## The stable contract

Every `SignaturePad` handle and every adapter exposes the same core controller:

```ts
import { SignaturePad, type SignaturePadHandle } from 'signetpad/react';
import { useRef } from 'react';

export function SignatureField() {
  const padRef = useRef<SignaturePadHandle>(null);

  function inspect() {
    const snapshot = padRef.current?.snapshot;
    const data = padRef.current?.toData();
    padRef.current?.undo();
    padRef.current?.clear();
    return { snapshot, data };
  }

  return (
    <div>
      <SignaturePad ref={padRef} aria-label="Agreement signature" />
      <button type="button" onClick={inspect}>
        Inspect
      </button>
    </div>
  );
}
```

Drop down to `useSignaturePad` only when the drawing surface is not a canvas (or not `SignatureSvg` on native).
