---
'signetpad': minor
---

Add ready-to-use `SignaturePad` components and automatic canvas rendering so the common integration is a few lines of code. Existing `useSignaturePad` hooks stay available for custom surfaces.
